name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: configure
      run: cmake -B build -DPCF8563_BUILD_TESTS=ON
    - name: build
      run: cmake --build build
    - name: test
      run: ctest --test-dir build --output-on-failure
